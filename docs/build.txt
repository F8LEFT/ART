How to build:

Before building this project, you must conform that you have installed
the latest Qt program(Qt5.7) and cmake.

git clone https://github.com/F8LEFT/ART

cd ART

mkdir build

cd build

git submodule init

git submodule update

mkdir build

cd build

cmake -DRELEASE=1 -DQTGCCDIR=${QTGCCDIR} -D[WIN32|APPLE|UNIX]=1 --build ..

make MainWindow

You can see generated files in lib/gui/bin.
QTGCCDIR is the compile toolkit from qt. For example,
